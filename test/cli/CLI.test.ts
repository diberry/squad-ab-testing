import { describe, it, expect } from 'vitest';

describe('CLI', () => {
  it('should parse experiment config path argument', () => {
    // TODO: Test config path parsing
  });

  it('should print results table after execution', () => {
    // TODO: Test end-to-end execution
  });

  it('should exit 0 on success, 1 on error', () => {
    // TODO: Test exit codes
  });
});
