import { describe, it, expect } from 'vitest';
import { Scorer } from '../../src/scoring/Scorer.js';

describe('Scorer', () => {
  it('should evaluate run with given evaluator', () => {
    // TODO: Test evaluator invocation
  });

  it('should store evaluation result per run', () => {
    // TODO: Test quality score storage
  });

  it('should handle evaluator errors gracefully', () => {
    // TODO: Test error handling and unevaluated marking
  });

  it('should aggregate quality scores across repetitions', () => {
    // TODO: Test aggregation of quality scores
  });
});
