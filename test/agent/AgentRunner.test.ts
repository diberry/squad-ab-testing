import { describe, it, expect } from 'vitest';
import { AgentRunner } from '../../src/agent/AgentRunner.js';

describe('AgentRunner', () => {
  it('should spawn agent with specific model', () => {
    // TODO: Test agent spawning with model override
  });

  it('should execute agent and capture output', () => {
    // TODO: Test agent execution and output capture
  });

  it('should capture token usage from agent run', () => {
    // TODO: Test token usage capture
  });

  it('should capture latency', () => {
    // TODO: Test latency measurement
  });

  it('should handle agent timeout', () => {
    // TODO: Test timeout handling
  });
});
